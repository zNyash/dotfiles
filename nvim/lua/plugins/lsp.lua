return {
  "neovim/nvim-lspconfig",
  opts = {
    inlay_hints = {
      enabled = false, -- disable inlay hints by default
      -- exclude = { "vue" }, -- optional: filetypes to exclude if you enable later
    },
  },
}
